# 구현 계획 (implementation-plan.md, §23·§24)

## 단계 (§23)
1. 프로젝트 분석 + 문서 ✅
2. 데이터 모델 + 관리자 구조 ✅(스키마), 진행(관리자 UI)
3. 공통 헤더/푸터/검색/SEO 컴포넌트
4. 가수 목록 + 프로필
5. 기사 목록 + 기사 상세
6. 공연 목록 + 공연 상세
7. 방송 일정 + 신곡 페이지 (경량)
8. OpenAI 이미지 API 연동 (설계+모듈)
9. 자동 수집·검증·초안 생성 (모듈)
10. 하루 최대 5개 예약 발행 (엔드포인트, 스위치 OFF)
11. 사이트맵·RSS·robots·구조화 데이터
12. 관리자 품질 검사 + 로그

## 이번 턴 범위 (§24) — 대표 페이지
- [x] 홈페이지
- [x] 가수 목록 `/artists`
- [x] 가수 프로필 1개 이상 `/artists/{slug}`
- [x] 최신 기사 목록 `/news`
- [x] 기사 상세 1개 `/news/{slug}`
- [x] 공연 목록 `/events`
- [x] 공연 상세 1개 `/events/{slug}`
- [x] 관리자 콘텐츠 편집 화면 `/admin/articles/{id}`
- [x] 자동 생성 테스트용 초안 1개 (autoGenerated draft, review 상태)

## 변경/생성 파일 예상 목록
```
package.json, tsconfig.json, next.config.mjs, tailwind.config.ts,
postcss.config.mjs, .gitignore, .env.example, .env.local
prisma/schema.prisma, prisma/seed.ts
src/app/{layout,page,globals.css}
src/app/artists/{page, [slug]/page}
src/app/news/{page, [slug]/page}
src/app/events/{page, [slug]/page, region/[region]/page}
src/app/broadcasts/page, src/app/music/[slug]/page, src/app/programs/[slug]/page
src/app/weekly/[yw]/page, src/app/search/page
src/app/admin/{layout, page, login/page, articles/page, articles/[id]/page}
src/app/api/{auth/login, auth/logout, corrections, cron}/route.ts
src/app/{sitemap.ts, robots.ts, rss.xml/route.ts, sitemap-*.xml/route.ts}
src/components/{Header,Footer,SearchBox,ArtistCard,ArticleCard,EventCard,
  Breadcrumbs,JsonLd,SampleBadge,SourceList,StatusBadge,SiteNotice}
src/lib/{db,auth,enums,json,seo,structured-data,sources,quality-score,
  image,automation,format,middleware-helpers}
src/middleware.ts
```

## 안전장치
- 자동 발행 스위치 `AUTO_PUBLISH_ENABLED=false` 유지 (§24).
- 모든 시드 데이터 `isSample=true` + 화면 SAMPLE 배지.
- 관리자 인증 미들웨어로 `/admin`·쓰기 API 보호.

## 다음 단계 권장
- 실제 공식 출처 커넥터(소속사/방송사 편성/티켓처 API·RSS) 연동.
- 링크 유효성/공연 상태 정기 점검 Cron.
- Postgres 이전 + 배포 파이프라인.
- 이미지 저장소(오브젝트 스토리지) + srcset 파이프라인.
